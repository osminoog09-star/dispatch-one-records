# Claude QA notes — LAPD Records

Дата аудита: 2026-08-05.

## Что было найдено и исправлено

- В публичной странице смен была dev-ссылка `+ тест-смена`. На живом сайте такие кнопки выглядят непрофессионально и путают игроков. Ссылка убрана из шаблона, route не тронут.
- В протоколах задержаний и списке дел выводились сырые ISO-даты вида `2026-08-05T22:26:21.2715787+03:00`. Теперь используются человекочитаемые даты через `created_fmt` / `game_time_fmt`.
- В судебных и arrest-документах проскакивал английский класс `Wobbler`. Он переведён как `по усмотрению суда`.
- В заметках pdComp встречались английские фразы (`arrested`, `at`, `vehicle plate`) и mojibake-адреса (`Рљ...`). Добавлена мягкая очистка при выводе без изменения исходных данных в базе.

## Что Claude не должен ломать

- Не редактировать `docs/` руками: это результат `python server/export_static.py`.
- Не трогать Supabase JS и inline-логику в `tickets.html`, `staff.html`, `dictionaries.html`.
- Не переименовывать JS-зависимые классы: `sb-edit`, `sb-badge`, `sb-rank`, `sb-dept`, `nav-drop`, `nav-menu`, `admin-only`, `auth-slot`, `tk-*`, `dic-*`.
- Не менять backend/import/launcher/agent без отдельной задачи.

## Что проверить после следующих правок

1. Все страницы открываются с кодом 200: `/`, `/cases`, `/case/19`, `/court/20`, `/shifts`, `/map`, `/register`, `/staff`, `/tickets`, `/dictionaries`.
2. В видимом HTML нет `Wobbler`, `arrested`, `CITATIONS`, `RECORDS`, `COURT`, `test-смена`.
3. В протоколах нет сырых игровых дат с `T22:26:21...+03:00`.
4. Карта остаётся кликабельной: hover по легенде подсвечивает сектор, клик выбирает сектор, статистика под картой не пропадает.
5. Авторизация Discord, тикеты, справочники, редактор персонала работают как раньше.

## Следующий аккуратный промпт для Claude

Проверь LAPD Records как production-интерфейс. Меняй только визуальную часть и безопасное форматирование вывода. Не трогай Supabase-логику, backend, SQL, launcher, agent и не редактируй `docs/` руками. Найди англоязычные хвосты, сырые технические даты, dev/test-элементы, mojibake, некрасивые пустые состояния и визуальные несостыковки. Всё, что можно исправить без риска для логики, исправь в `server/app/static/style.css` и Jinja-шаблонах. После этого запусти `python server/export_static.py`, проверь ключевые страницы на 200 и сделай `rg` по `docs/` на `Wobbler|arrested|CITATIONS|RECORDS|COURT|test-смена|T[0-9]{2}:[0-9]{2}:[0-9]{2}`.
