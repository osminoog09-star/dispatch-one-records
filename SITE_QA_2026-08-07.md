# Site QA 2026-08-07

## Scope

Проверка публичного сайта LAPD Records после жалоб на обрезанный текст, английские хвосты, битые связи и пустые/непонятные страницы.

## Fixed

- В `server/app/db.py` расширена локализация судебных данных из pdComp.
- Переведены хвосты вроде `Private Counsel`, `Self-Represented`, `No contest`, `Fix-It`, `Fine paid`, `United States v.`, `Probation terms`, `Appeal denied`, `Motion to dismiss`.
- Хронология личных дел теперь локализует судебные поля (`sentence`, `source`, `plea`, `judge`, `prosecutor`, `defense`, `courtroom`) перед выводом связанных событий.
- `docs/` пересобран через `python server/export_static.py`.

## Verified

- `python -m py_compile server/app/db.py server/app/main.py server/app/discord_post.py agent/pdcomp_sync.py server/export_static.py`
- `python server/export_static.py`
- Flask smoke: 24 ключевых маршрута отдают `200`.
- Static link check по `docs/**/*.html`: `missing_count 0`.
- Target visible-English check по `docs/**/*.html`: `target_hits 0`.

## Notes For Claude

- PowerShell на этой машине часто показывает кириллицу как мусор в консоли. Это не всегда порча файла. Проверять UTF-8 нужно через Python, например `Path(file).read_text(encoding="utf-8")`, а не глазами по выводу PowerShell.
- `docs/` не править руками. Только через `python server/export_static.py`.
- Основной следующий продуктовый шаг: связанные карточки. Пользователь хочет, чтобы из любого места можно было открыть связанные аресты, вызовы, суды, штрафы, карту и хронологию.
- Второй следующий шаг: статистика за 24 часа / неделю / месяц и смены по 12 часов, плюс отправка отчётов смен в Discord.
